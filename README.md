# CarteAuxTresors Backend

This project was generated with express-generator

## Start backend (after frontend build)

Run `npm start` to build the project into the backend public folder.

## Run Jasmine unit tests

Run `npm test`
